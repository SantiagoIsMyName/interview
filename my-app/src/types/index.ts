// Add types for AI Phone Screen application
// TODO: Candidates should expand and modify these types based on their implementation

export interface Call {
    firstName: string
    lastName: string
    phoneNumber: string
    callId: string
    status: string
    createdAt: string
}